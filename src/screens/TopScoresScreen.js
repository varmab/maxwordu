import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import TopScores from '../components/TopScores'

const TopScoresScreen = (props) => {
    return (
        <TopScores {...props} />
    )
}

export default TopScoresScreen

const styles = StyleSheet.create({})